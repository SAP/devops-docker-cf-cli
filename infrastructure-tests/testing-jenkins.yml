jenkins:
  numExecutors: 10
jobs:
  - script: >
      multibranchPipelineJob('infrastructure-integration-test') {
          branchSources {
              git {
                  remote("https://github.com/piper-validation/cloud-s4-sdk-book")
                  includes('infrastructure-integration-test')
              }
          }
      }
credentials:
  system:
    domainCredentials:
      - credentials:
          - usernamePassword:
              scope: GLOBAL
              id: "devops-docker-images-IT-cf"
              username: ${CX_INFRA_IT_CF_USERNAME}
              password: ${CX_INFRA_IT_CF_PASSWORD}
              description: "SAP CP Trail account."

